# Device matrix

Only completed physical runs belong in the verified column.

| Device | OS | API | Scope | Status |
|---|---|---:|---|---|
| Xiaomi 14T Pro | HyperOS 3 | TBD on device | 50 landscape + 20 portrait cold starts, rotation, lifecycle stress, capture parity | Pending (`Rana-6vh`, `Rana-dag`) |

Automated host/JVM tests and debug APK builds are not substitutes for a physical
camera result. Add the exact build commit, OS build, API level, auto-rotate
setting, iteration count, failures, and captured diagnostic snapshot when a run
is completed. Do not generalize a pass to an untested device family.
